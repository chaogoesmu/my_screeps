var runRoom = {

    /** @param {Creep} creep **/
    run: function(RoomName) {
        //go through all the creeps and find the ones under control for this room
		var ThisRoomsCreeps = Game.creeps.filter(creep => creep.memory.room == RoomName;
		for(creep in ThisRoomsCreeps)
		{
			//run the code and tally the things
		}
		//respawn code here
		
	}
}

module.exports = runRoom;
